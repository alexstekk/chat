{
  "name": "chat",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "homepage": "https://alexstekk.github.io/chat/",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "vite build --base=/chat/",
    "predeploy": "rimraf dist && npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "rimraf": "^6.0.1",
    "sass-embedded": "^1.86.3",
    "vite": "^6.2.0"
  }
}
